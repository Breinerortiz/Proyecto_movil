name: proyecto_final_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
 
 
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  firebase_storage: ^13.3.0

  # Cámara / imágenes
  image_picker: ^1.2.1
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  fl_chart: ^0.68.0

  google_maps_flutter: ^2.13.1
  

  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true